package com.ausky.training.algorithm.sort;

/**
 * 排序接口
 */
public interface Sort {
    /**
     * 数组进行排序，返回排序后的数组
     *
     * @param targetArr
     * @return
     */
    int[] sort(int[] targetArr);
}
