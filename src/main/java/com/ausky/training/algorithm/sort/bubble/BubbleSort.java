package com.ausky.training.algorithm.sort.bubble;

import com.ausky.training.algorithm.sort.Sort;

public class BubbleSort implements Sort {
    public int[] sort(int[] targetArr) {
        return new int[0];
    }
}
